import { PoredjajPoPipe } from './poredjaj-po.pipe';

describe('PoredjajPoPipe', () => {
  it('create an instance', () => {
    const pipe = new PoredjajPoPipe();
    expect(pipe).toBeTruthy();
  });
});
